        <div class="tab-pane fade" id="achievement" role="tabpanel" aria-labelledby="achievement-tab">
                {% include 'admin/achievement_form.html.twig' %}
                        <h3>All rules:</h3>
                <table id="achievementsTable" class="table table-striped projects">
                    <thead>
                        <tr>
                            <th style="width: 20%">Badge Name</th>
                            <th style="width: 20%">Rule Name</th>
                            <th style="width: 20%"></th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for achievement in achievements %}
                        <tr>
                            <td>
                                {{ achievement.badge.getName is defined ? achievement.badge.getName : 'N/A' }}
                            </td>
                            <td>
                                {{ achievement.achievement.getName is defined ? achievement.achievement.getName : 'N/A' }}
                            </td>
                            <td>
                                <a href="/achievement/edit/{{ achievement.getId }}" class="btn btn-info btn-xs">Edit</a>
                                <button type="button" data-id="{{achievement.getId}}" class="btnDelete btn btn-danger btn-xs">Delete</i></button>
                            </td>
                        </tr>
                        {% endfor %}
                </table>
        </div>
{% block customjs %}
<link href="{{ asset('web/customcss/style.css') }}"  rel="stylesheet">
{% endblock %}